## 变更范围

-

## 验证

-

## 检查清单

- [ ] 变更范围只包含本 PR 目标，没有混入无关格式化或缓存文件
- [ ] 已按风险运行相关测试或检查，并在上方写明命令和结果
- [ ] 没有提交 `.env`、token、成员数据、数据库导出、生产日志或本机绝对路径
- [ ] 文档默认使用中文，必要的协议名、配置键、命令和代码标识保留原文
- [ ] 如涉及契约、schema 或 payload，已先处理对应的 `big-apple-contracts` 变更
