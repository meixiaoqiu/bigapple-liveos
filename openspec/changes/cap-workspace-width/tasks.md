## 1. 竖版页面外壳

- [x] 1.1 将 Workspace 首页页头和主体限制为 `480px` 最大宽度并在宽屏居中，同时保持窄屏使用完整可用宽度
- [x] 1.2 移除首页主要区域的视口驱动多列断点，使现有模块在所有终端保持单列顺序且不删除任何内容

## 2. 回归覆盖与文档

- [x] 2.1 增加 Workspace 页面布局测试，验证 480px 外壳、页头主体对齐、单列规则和现有入口保留
- [x] 2.2 更新成员 Workspace 产品文档，记录 480px 竖版首页外壳及渐进改造边界

## 3. 构建与验证

- [x] 3.1 重新构建 Tailwind CSS 并确认生成产物完整
- [x] 3.2 运行相关 Workspace 测试、Django check、OpenSpec strict validation 和两个仓库的 `git diff --check`

## 4. 连续应用画布

- [x] 4.1 将页头和主体合并进至少满屏高的 480px 白色画布，同时保留画布外灰色背景
- [x] 4.2 更新布局测试和产品文档，重新构建 Tailwind CSS 并运行定向验证
