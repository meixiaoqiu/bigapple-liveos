## 1. 最小可用性判断

- [x] 1.1 在考试领域服务中实现 `ready`、`no_active_policy` 和 `insufficient_questions` 三种无副作用结果，并用单元测试验证政策与题目数量边界。
- [x] 1.2 让申请页 GET 和开始考试服务复用同一判断；测试未开放时统一显示“执衡者资格考试暂未开放，请稍后再试”、禁用按钮且不创建尝试。
- [x] 1.3 保留 POST 重检，测试页面加载后政策失效或题目减少时仍失败关闭。

## 2. 仿真重置恢复

- [x] 2.1 实现仅限 simulation 的幂等考试基线确保函数，创建一题仿真基线和一项有效政策；验证重复执行不重复数据。
- [x] 2.2 接入 simulation 初始化和 zero-start reset，验证迁移记录保留但考试数据清空后可以恢复并开始考试。
- [x] 2.3 测试已有有效配置时不覆盖题目、政策或考试历史，且 real world 不自动发布示例考试。

## 3. 管理员最小配置入口

- [x] 3.1 增加 world-scoped workspace 配置页及导航入口，仅允许具有 `governance.manage_deliberator_exam` 的当前有效成员访问。
- [x] 3.2 增加创建并发布单选题的最小表单，复用现有领域服务，测试普通成员、Django staff/superuser 和跨 world 请求被拒绝。
- [x] 3.3 增加抽题数量和及格百分比政策发布表单，测试题目不足、唯一有效政策和发布后无需重启即可开始考试。
- [x] 3.4 验证普通成员页面和响应不暴露正确答案、解释或内部评分快照。

## 4. 验证与文档

- [x] 4.1 更新直接相关的主规格和 Docs 管理员、执衡者考试及仿真重置说明，不扩展启动诊断或运维平台文档。
- [x] 4.2 运行项目检查、Django check、迁移漂移检查及考试、权限和 world-reset 定向测试。
- [x] 4.3 运行完整隔离回归、Docs 中英文构建、OpenSpec strict 和两个仓库的 `git diff --check`。
- [x] 4.4 人工验收 simulation 重置后可以考试、real 未配置时显示统一提示、管理员可从 workspace 完成首次配置。
