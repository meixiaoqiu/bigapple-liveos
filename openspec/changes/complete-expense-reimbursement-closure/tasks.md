## 1. 契约与现状基线

- [x] 1.1 再次核对 `bigapple-docs/static/technical-contracts/` 是否存在报销或附件公共 API；若实施引入 API/schema/payload 变化，先更新对应契约并通过契约校验，否则记录“仅 Django 页面和内部模型，无公共契约变化”。
- [x] 1.2 为现有报销、头像文件处理、Storage 配置、world runtime 清理和财务权限建立实施前测试基线，确认未提交改动不被覆盖。

## 2. 审计型业务附件模型

- [x] 2.1 在 `core.models.attachments` 新增不可变 `Attachment`，包含稳定 ID、对象 key、检测类型、显示文件名、字节数、SHA-256、上传人、受众、生命周期、来源/更正关系和时间，并为非显然字段填写 `help_text`。
- [x] 2.2 新增带真实外键的 `ExpenseClaimAttachment`，区分支出凭证、付款凭证和公开材料，约束私有原件、公开副本来源及与 `PaymentExecution` 的关系，不使用 `GenericForeignKey`。
- [x] 2.3 生成迁移并测试历史报销可无附件继续读取、新记录约束生效、附件元数据和已密封关联不能原地改写、跨 world 数据库不会互读。

## 3. 文件验证与私有存储

- [x] 3.1 从现有头像代码提取或复用流式大小限制、Magika 检测、图片安全解码和 SHA-256 原语，增加 PDF/CSV 的窄验证策略；集中配置允许类型、单文件、数量和总大小限制并补齐伪装、损坏和资源超限测试。
- [x] 3.2 增加业务附件 key 与 Storage gateway，使用 `<world-id>/runtime/permanent-attachments/` 下不可预测且不可覆盖的 key，删除能力按明确临时/未提交状态限制，证明头像清理不能触及业务附件。
- [x] 3.3 实现附件创建、更正和公开副本发布领域服务，所有权威状态变化函数带 docstring；测试对象写入失败、数据库回滚补偿、旧版本保留、私有原件不能切换为公开及未授权发布失败。
- [x] 3.4 扩展存储审计命令以报告缺失权威对象、无引用对象、未完成临时对象和非法生命周期引用；默认 dry-run，清理模式不得删除密封附件或其它 world 对象。

## 4. 报销提交与私有读取

- [x] 4.1 扩展报销表单为 `multipart/form-data`，加入 `public_note` 和多文件支出凭证控件，保持 daisyUI 样式并在 Tailwind class 变化后重新构建 `theme/static/css/dist/styles.css`。
- [x] 4.2 扩展 `submit_expense_claim()`，原子编排报销、至少一份凭证、附件关联、公开 `Event` 和 `SystemEvent`；测试无凭证、任一文件失败、重复提交和数据库失败均不产生半完成报销或孤儿对象。
- [x] 4.3 增加报销附件受控下载路由，以具体 `ExpenseClaim` 为授权上下文；测试申请人、审核人、付款人、`finance.view_private`、无关成员、匿名访客和跨 world 请求的允许/拒绝矩阵。
- [x] 4.4 在报销详情显示有权查看的附件和连续时间线，普通成员仍不能查看他人报销，撤回后停止后续审核/付款且保留内部证据审计边界。

## 5. 可替换付款执行

- [x] 5.1 新增不可变 `PaymentExecution` 模型和迁移，记录报销、后端类型、状态、付款日期、方式、实名付款人、备注、外部引用、同步状态、白名单结果快照和时间，并建立与 `FinanceTransaction`/付款凭证的稳定关联。
- [x] 5.2 定义窄 `FinancePaymentBackend` 协议、注册表和 `LiveOSManualPaymentBackend`；默认配置只启用内置后端，未知或未实现后端在 system check/使用时失败关闭，不创建第三方占位实现。
- [x] 5.3 增加付款表单的日期、方式、备注和至少一份付款凭证，扩展 `mark_expense_claim_paid()` 原子协调授权、后端结果、附件、`PaymentExecution`、报销状态、追加式流水及两类事件。
- [x] 5.4 测试只有已批准报销可付款、申请人不能自付、缺少凭证失败、后端异常失败关闭、敏感返回不进入快照、成功操作恰好生成一条执行记录和一条不可修改流水。

## 6. 公开脱敏副本与观察台

- [x] 6.1 实现仅由明确财务私有/发布权限触发的公开副本上传与发布流程，独立保存对象和记录并关联来源原件；测试未发布副本、撤回/作废副本和私有原件均不能通过公开路由读取。
- [x] 6.2 扩展公开财务页和报销详情，白名单展示用途、金额、支出日期、收款方公开名称、申请人、审核人、付款状态、审核理由和公开时间线，仅为已发布公开副本提供受控下载。
- [x] 6.3 添加隐私回归测试，证明页面、事件、日志和响应不含账户、联系方式、内部备注、私有文件名、对象 key、哈希、后端原始响应或存储凭据。

## 7. Admin、world 生命周期与演示闭环

- [x] 7.1 在 Django Admin 中只读展示附件、报销附件关联和付款执行元数据，禁止绕过领域服务新增、编辑或删除权威记录，并更新相应 Admin 权限测试。
- [x] 7.2 验证仿真 world 重置清理该 world 的 runtime 业务附件且不访问 `SimulationSnapshot` 或 `var/simulation_archives/`；Storage 清理失败时仍禁止数据库 flush，真实 world 和其它 world 不受影响。
- [x] 7.3 扩展 demo/仿真验收数据或新增窄 smoke workflow，完成“提交 100 元报销与支出凭证 → 独立审核 → 第三人确认付款与付款凭证 → 追加式流水 → 发布公开副本 → Observer 展示”的可重复验证。

## 8. 文档与完整验证

- [x] 8.1 更新 `bigapple-docs` 的数据库结构、架构总览、成员工作台、观察台、Admin 和开发说明，明确“当前全部由 Live OS 内置实现、付款能力未来可替换、制度事实和档案不外移”；如无 API 变化，文档明确记录技术契约无需修改。
- [x] 8.2 运行附件、财务、workspace、observer、Admin、world reset 和授权 targeted tests，随后运行完整 Django 回归、`scripts/check_project.py`、system check、迁移 dry-run、OpenSpec strict validation 和两个仓库的 `git diff --check`。
- [x] 8.3 在真实 OCI 私有桶执行随机业务附件 Put/Head/Get/Delete 安全探针；在隔离回归测试中自动创建申请人、审核人、付款人和公开发布人四名虚拟成员并跑通报销闭环；对 `simulation0001` 执行 runtime 清理 dry-run。任何验收不得输出凭据、bucket、对象 key 或私密凭证内容，也不得为了测试向开发数据库授予高信任角色或写入伪造财务记录。

## 9. 安全修复

- [x] 9.1 新增独立 `finance.publish_public_attachments` 权限和公开发布人角色，确保审核、付款和仅私有查看职责均不能发布公开副本。
- [x] 9.2 将对象存储补偿扩展至附件服务内部失败及调用方外层数据库事务后续失败，补偿不得依赖未回滚事务中的附件记录可见性。
- [x] 9.3 使用申请人、审核人、付款人和公开发布人四名虚拟成员跑通回归流程，并覆盖越权发布和数据库回滚后无孤儿对象。
- [x] 9.4 为附件批次服务增加自身数据库原子边界，覆盖公开发布和更正入口中“附件元数据成功、业务关联失败”的回滚，并验证数据库记录与对象均无残留。
