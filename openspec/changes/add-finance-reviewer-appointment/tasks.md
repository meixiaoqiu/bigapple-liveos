## 1. 权限和领域服务

- [x] 1.1 核对公共技术契约不受影响，并为既有财务角色、角色任命提案和授权边界建立测试基线。
- [x] 1.2 实现财务审核职责提名服务，固定目标角色、校验 `governance.manage_roles` 和目标守约者前置条件。
- [x] 1.3 实现受权限保护的任命执行封装，复用既有提案执行并拒绝未通过或错误类型/角色的提案。
- [x] 1.4 测试普通成员、仅 Django 技术管理员、非守约者目标、重复提名和越权执行均失败关闭。

## 2. 成员工作台

- [x] 2.1 增加财务职责管理页面，展示候选守约者、待表决提案、可执行提案和当前审核者。
- [x] 2.2 增加提名、投票和执行 POST 操作；actor 只从登录 Member 推导，角色和目标提案均由服务端限定。
- [x] 2.3 增加匿名、普通守约者、典守者但非执衡者、跨提案类型、伪造角色和重复提交测试。
- [x] 2.4 更新工作台导航并重新构建 Tailwind 产物。

## 3. 端到端仿真前置链路

- [x] 3.1 在隔离测试数据库跑通首位守约者考试成为执衡者、新账号报名、表决准入和执行。
- [x] 3.2 通过财务审核职责提案完成提名、投票和执行，验证仅产生审核与私有材料查看能力。
- [x] 3.3 使用另一名成员提交带证据的报销，由新审核者批准，并验证禁止自审和未任命前不能审核。
- [x] 3.4 增加 realworld、simulation0001 和 control 数据库计数隔离验证。

## 4. 文档和部署验证

- [x] 4.1 更新架构、治理边界、成员工作台、仿真和开发文档，说明任命路径和最小授权边界。
- [x] 4.2 对两个 world 重建 OpenFGA tuples，并在仿真 world 以已有账号实际完成前置链路和报销审核。
- [x] 4.3 运行定向测试、完整 Django 回归、项目检查、system check、迁移 dry-run、OpenSpec strict validation 和两个仓库的 diff check。

## 5. 终审生命周期与并发修复

- [x] 5.1 收紧 OpenFGA 角色任命增量投影：仅写当前有效任命、撤销后删除 tuple，并在授权时以 Django 当前任命事实否决未来、到期、撤销或资格失效的陈旧 tuple。
- [x] 5.2 为提案投票建立统一事务和提案行锁，验证事件或状态计算失败时投票、事件与提案状态全部回滚。
- [x] 5.3 在财务审核者提名事务中锁定目标成员并复查开放提案，验证并发请求最多创建一项开放任命提案。
- [x] 5.4 页面候选人与当前审核者复用统一当前权限查询，排除未来、到期、失效角色和失效成员，并增加 workspace 回归测试。
- [x] 5.5 更新 OpenFGA 生命周期设计说明，运行定向测试、完整回归、项目检查、迁移检查、OpenSpec 严格校验和两个仓库 diff check。
