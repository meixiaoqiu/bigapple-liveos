## 1. 契约、现状与安全基线

- [x] 1.1 核对技术契约不存在执衡者考试公共 API；记录本次仅新增 Django workspace/Admin 和内部模型，如实施引入公共 API/schema/payload 则先更新 sibling 技术契约。
- [x] 1.2 为现有执衡者任期、守约者准入选民规则、RoleAssignment/OpenFGA、world 路由、Admin 业务授权和仿真角色基线建立实施前测试基线，并保留当前未提交改动。

## 2. 题库、政策与考试权威模型

- [x] 2.1 在 `core.models` 的独立领域模块新增 `DeliberatorExamPolicy`、`DeliberatorExamQuestion` 和 `DeliberatorExamAttempt`，包含稳定 ID、版本、状态、生效时间、题目/评分私有快照、作答、得分、结果、责任人、关联任期和审计时间，并为非显然字段填写 `help_text`。
- [x] 2.2 增加数据库约束和模型/服务校验：单一当前政策、题目版本唯一、选项至少两个且 ID 唯一、唯一正确选项、正分值、抽题数为正、及格线有效、Attempt 结果与提交时间/任期关系一致。
- [x] 2.3 生成非破坏性迁移并初始化抽取一题的默认政策和一道可替换示例题；验证历史执衡者任期不变、重复迁移稳定、real/simulation world 数据隔离且 control DB 不承载 world 考试事实。

## 3. 题库维护权限与领域服务

- [x] 3.1 新增 `governance.manage_deliberator_exam` 权限并绑定规范典守者角色，更新权限基线和 OpenFGA 投影测试；证明普通成员、仅执衡者和只有 Django 技术账号标记的用户不能维护题库。
- [x] 3.2 实现题目草稿创建、发布、版本化替换和停用服务，以及政策发布/替换服务；权威状态变化函数写 docstring，并在统一事件账本中只记录安全元数据，不写正确答案和题目全文。
- [x] 3.3 在 control plane Admin 增加政策和题库维护入口，按绑定 Member 的业务权限控制新增/修改/发布/停用；已发布版本和考试尝试只读，禁止后台改分、改通过状态或直接创建任期。
- [x] 3.4 测试第一题发布、已发布题目版本化更改、旧 Attempt 快照不变、题量不足失败关闭、非法选项/正确答案/政策参数拒绝和跨 world 维护拒绝。

## 4. 随机组卷、评分与原子任命

- [x] 4.1 实现开始考试服务：重新验证有效守约者、账号和现有任期，锁定当前政策，从有效题目中使用服务端安全随机源无重复抽取，并原子保存私有快照；支持单题题库和测试抽样器。
- [x] 4.2 实现提交考试服务：锁定 Attempt、校验归属和完整答案、忽略或拒绝客户端评分字段、按快照评分并写入一次性最终结果；普通读取投影不得包含正确答案、解释或内部评分快照。
- [x] 4.3 收紧现有 `apply_for_deliberator_term()` 绕过路径，把考试通过与一年期 RoleAssignment、关联 Attempt 和角色事件放入同一事务；测试评分/任命任一步失败均无半完成状态。
- [x] 4.4 增加并发与幂等测试：重复提交同一 Attempt、同一成员并发通过多个 Attempt、已有任期重新考试、考试中守约者资格/账号失效均最多产生一届 active 任期且保留明确失败结果。
- [x] 4.5 更新既有执衡者服务、角色目录、身份展示、OpenFGA 和提案选民测试，证明通过后的成员可以参与守约者准入表决，未通过者、典守者但非执衡者和到期任期仍不能投票。

## 5. 成员工作台申请体验

- [x] 5.1 在 workspace 增加执衡者资格卡片和路由：按成员状态显示前置条件、开始考试、当前尝试、结果或有效任期；身份只从登录 User 绑定 Member 推导，不接受 URL/payload 选择成员。
- [x] 5.2 实现服务端生成的单选考试表单和提交页面，使用当前 Attempt 快照渲染题目，CSRF/POST 校验失败关闭；结果页只显示得分、及格线、结果和新任期，不泄露正确答案。
- [x] 5.3 增加 workspace 权限、篡改和隐私测试：匿名、贡献者、停用成员、他人 Attempt、跨 world Attempt、伪造题目/选项/分数/通过状态均被拒绝；单题正确通过、错误未通过和再次考试流程可用。
- [x] 5.4 如修改模板 Tailwind class，运行 Tailwind build 并提交生成的 `theme/static/css/dist/styles.css`；验证窄屏、错误提示和题库不可用状态可理解。

## 6. 仿真解锁与端到端前置链路

- [x] 6.1 更新角色基线、seed 和相关测试，不再直接调用无考试的执衡者任命服务；在隔离测试库中让初始化守约者通过单题考试取得首位执衡者任期。
- [x] 6.2 增加端到端回归：首位守约者考试通过成为执衡者 → 新账号注册并提交守约者申请 → 首位执衡者投票使申请通过并执行 → 新成员获得守约者资格；证明仿真 world 不再卡在零执衡者状态。
- [x] 6.3 验证上述链路不会自动授予新成员财务角色、不会自动审核报销，并为后续“任命财务审核者并完成报销审核”保留独立任务边界。

## 7. 文档、部署与完整验证

- [x] 7.1 更新 `bigapple-docs` 的数据库结构、架构总览、治理边界、成员工作台、Admin、仿真和开发说明，记录题库维护权限、考试门槛、首位执衡者恢复路径及无公共契约变化。
- [x] 7.2 应用 realworld 和 simulation world 迁移，初始化默认政策/第一题并重建 OpenFGA tuples；不得输出正确答案、数据库凭据或私密考试快照，不得为测试向开发数据库写入伪造通过记录。
- [x] 7.3 运行题库、考试、角色、准入、workspace、Admin、OpenFGA、world 隔离和仿真 targeted tests，随后运行完整 Django 回归、`scripts/check_project.py`、system check、迁移 dry-run、OpenSpec strict validation 和两个仓库的 `git diff --check`。

## 8. 终审并发修复

- [x] 8.1 为生效考试政策增加 MySQL 可执行的数据库唯一槽位与一致性约束，发布冲突转换为领域错误，并增加迁移和并发/约束回归测试。
- [x] 8.2 重新运行考试定向测试、迁移检查、完整回归与 OpenSpec 严格校验。
