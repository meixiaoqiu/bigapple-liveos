## 1. 投影行为测试

- [x] 1.1 为成员任务、申诉、治理提案和采购事项补充统一投影测试，验证稳定标识、状态、责任、当前处理方、动作、更新时间和目标入口。
- [x] 1.2 补充“需要我处理”“等待他人”“最近结束”分组、开放任务排除、权限可见性和内部 metadata 不泄露测试。

## 2. 事务聚合实现

- [x] 2.1 扩展 Workspace 工作项构建器，以有界查询和现有领域权限判断生成只读统一事项字典，不新增持久化模型或授权路径。
- [x] 2.2 将统一事项按成员行动关系排序和分组，对无法确认的责任信息采用明确降级值，并接入 `workspace_context`。

## 3. 首页迁移布局

- [x] 3.1 在 Workspace 首页旧有业务模块之前增加“我的事务”区域，渲染三个分组、事项最小信息、现有操作入口和空状态。
- [x] 3.2 保持变更前全部首页模块、表单、按钮、链接和权限条件存在且功能不变，仅将其下移，并增加页面顺序与旧模块保留回归测试。
- [x] 3.3 运行既有 Tailwind 构建，核对并保留所需的生成样式变更。

## 4. 文档与迁移清单

- [x] 4.1 在 `bigapple-docs` 同步 Workspace 统一事务投影、领域边界和迁移期旧模块保留规则。
- [x] 4.2 建立交付用旧模块待确认清单，逐项说明其与新事务区域的重叠关系，不在本次删除任何模块。

## 5. 验证

- [x] 5.1 运行 Workspace 相关测试和权限故障回归，修复所有本次变更导致的失败。
- [x] 5.2 运行 Django 基础检查、迁移漂移检查和 `git diff --check`，确认无 API、schema、payload、数据库迁移或无关改动。

## 6. 终审修复

- [x] 6.1 修复提案和采购候选在权限或可处理性判断前截断导致的待办遗漏，并增加第 21 条仍可被发现的回归测试。
- [x] 6.2 将未实际分派的提案处理人改为责任角色展示，并增加不得把查看者显示为处理人的回归断言。
- [x] 6.3 为申诉关联成员增加预加载并通过查询捕获测试防止逐条关联查询。
- [x] 6.4 重新运行 Workspace 测试、基础检查、迁移漂移检查、OpenSpec 严格验证和 diff 检查。

## 7. 终审性能与跨来源排序修复

- [x] 7.1 将提案和采购事项改为各来源独立收集后统一排序限量，增加较新的第二条采购事项不被其他来源配额挤掉的回归测试。
- [x] 7.2 为提案与采购定义每来源有界候选预算，批量加载审批决定和报价采纳提案，避免无上限扫描及逐项权限查询。
- [x] 7.3 增加候选查询有上限和查询次数不随候选条数线性增长的回归测试。
- [x] 7.4 重新运行 Workspace 测试、基础检查、迁移漂移检查、OpenSpec 严格验证和 diff 检查。

## 8. 终审数据库筛选与旧行为恢复

- [x] 8.1 移除固定 100 条候选窗口，使用数据库审批决定条件筛选当前成员真实可处理提案，并覆盖第 101 条场景。
- [x] 8.2 使用数据库采纳提案状态筛选真实待验收报价，并覆盖前 100 条不可验收而第 101 条可验收的场景。
- [x] 8.3 恢复旧待执行模块按 `resolved_at` 排序及旧模块完整待办集合，新增排序回归测试。
- [x] 8.4 验证待办查询不产生逐项审批或采纳提案查询，并重新运行相关领域与 Workspace 回归。

## 9. 终审来源限量与最新提案语义修复

- [x] 9.1 在四类真实待办查询完成数据库筛选和排序后各截取前 20 条，同时通过独立计数保留旧模块的完整待办数量。
- [x] 9.2 将采购验收资格改为只判断同一报价的最新采纳提案状态，并覆盖历史提案已执行但最新提案未执行的回归场景。
- [x] 9.3 重新运行 Workspace 与提案领域测试、基础检查、迁移漂移检查、OpenSpec 严格验证和 diff 检查。
