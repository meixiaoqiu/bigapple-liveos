## 1. 契约与现状基线

- [x] 1.1 核对 `ApprovalProposal`、`ApprovalDecision`、`MemberApplication`、角色任命服务、OpenFGA 模型及 workspace 失败关闭入口，形成当前调用与数据依赖清单，并证明没有旧 `Proposal` 运行时依赖。
- [x] 1.2 先更新 sibling Docs 技术契约，定义统一提案、规则版本、选民快照、实名票据、判定证据和执行记录的 schema/payload；运行 JSON 与 OpenAPI 校验。
- [x] 1.3 固化提案类型适配器接口及当前类型迁移矩阵，明确成员准入使用新选民策略、既有采购暂用审批策略、其他登记流程继续失败关闭。

## 2. 权威数据结构

- [x] 2.1 扩展 `ApprovalProposal` 状态机与字段，保存策略类型、规则版本、冻结阈值、最低参与、截止、未决处理、判定和执行时间；为含义不明显字段补充中文 `help_text`。
- [x] 2.2 新增选民规则模板与不可变发布版本模型，使用受验证的稳定选择器类型表达角色、派生状态、专业资格、组合和排除条件，并记录实名发布者。
- [x] 2.3 新增不可变选民快照、只追加票据修订、判定证据和幂等执行记录模型，增加唯一约束、外键保护和必要索引。
- [x] 2.4 为 `MemberApplication` 增加指向唯一有效准入提案的明确来源关系或等价受约束关联，不复制报名权威状态。
- [x] 2.5 更新干净迁移基线及 schema 断言；从空的 control、real、simulation 测试数据库迁移后确认没有旧提案表或字段，也不要求转换旧提案数据。

## 3. 规则发布与选民评估

- [x] 3.1 实现规则配置 schema 验证，只允许登记的选择器、`ALL`、`ANY` 和明确排除条件，拒绝原始查询、可执行代码、未知角色名称和前端自定义表达式。
- [x] 3.2 实现规则草拟、发布和版本不可变服务；发布必须检查 `governance.manage_proposal_policies`，状态变化服务写中文 docstring 和实名审计事件。
- [x] 3.3 实现从冻结规则版本生成逐成员选民快照，覆盖贡献者派生状态、规范角色、专业资格和申请人排除，并保证重复生成幂等。
- [x] 3.4 实现投票前当前资格复核与结构化解释结果，证明快照成员资格失效后被拒绝、快照后新合格成员不会静默加入。
- [x] 3.5 增加角色目录扩展测试：新增已支持目录角色后只更新规则数据即可参与评估，提案核心不增加角色专用分支。

## 4. 统一提案生命周期

- [x] 4.1 重构统一提案服务和类型适配器注册表，完成草拟、开始表决、判定、执行与终态保护；禁止 view、Admin 或调用方直接改状态。
- [x] 4.2 在服务层分别检查发起、投票、规则发布、判定和执行权限，更新权限目录与 OpenFGA 模型；OpenFGA 异常或拒绝时不得回退到 Django 角色查询。
- [x] 4.3 实现实名投票与改票的只追加修订，在事务锁下确保每名成员只有一个当前有效选择，并测试并发提交不会产生双重有效票。
- [x] 4.4 实现基于冻结参数的通过、拒绝、过期与最低参与计算，保存输入计数和结果证据；截止后拒绝投票。
- [x] 4.5 实现适配器执行编排与幂等执行记录，只有已通过提案和有权执行者可调用，失败不得留下部分业务状态。
- [x] 4.6 更新事件 payload、统一事件账本与公开安全投影，保留实名责任但不得泄露报名私密字段、内部规则配置或 OpenFGA 标识。

## 5. 成员准入适配器

- [x] 5.1 实现成员准入适配器及幂等键：有效贡献者报名保存后关联唯一准入提案；重复请求返回同一提案。
- [x] 5.2 实现无有效准入政策时的等待配置状态，允许保存报名但禁止开始表决、直接批准、拒绝或执行，并返回明确提示。
- [x] 5.3 实现已发布准入政策的规则冻结、选民快照和申请人自决排除；测试管理员只有在满足选民规则时才能投票。
- [x] 5.4 实现通过后的授权执行，通过既有角色任命服务创建一年期守约者任期并更新报名结果；禁止直接创建 `RoleAssignment` 或附带其他角色与财务职责。
- [x] 5.5 实现拒绝、过期及执行失败的报名状态映射，记录实名投票、判定依据、执行者和失败原因，不删除报名或提案历史。
- [x] 5.6 测试申请人停用、退出、已有有效守约者资格、冲突任期、重复执行和适配器异常时全部失败关闭且无部分写入。

## 6. Workspace、Admin 与仿真入口

- [x] 6.1 增加管理员 workspace 准入政策配置页，使用同一服务发布角色集合、阈值、期限和未决处理；无权限成员和 Django staff 不得绕过业务权限。
- [x] 6.2 更新报名提交、列表和详情页，分别为申请人、合格选民和有权执行者展示同一权威状态、资格解释、截止与可用动作。
- [x] 6.3 增加投票、改票、判定和执行 POST 入口，所有入口绑定当前登录 Member、启用 CSRF、防重复提交，并在服务层重新授权。
- [x] 6.4 将 Django Admin 保持为只读审计与诊断入口，不允许直接新增票据、修改提案状态、发布规则或执行准入。
- [x] 6.5 更新 observer 的报名事项时间线，展示公开安全的提案阶段、实名公开决定和执行结果，不展示伪造进度或私密报名内容。
- [x] 6.6 更新 simulation 明确测试政策 seed 与 reset，保证幂等恢复可测试准入闭环且 real world 不自动发布示例政策。

## 7. 关闭边界与回归测试

- [x] 7.1 保留并扩展旧提案残余检查，证明新实现没有恢复旧模型、旧服务、旧 URL 或兼容门面。
- [x] 7.2 为角色任免、财务职责任命、社区共议、守约事务、专业事务、管理事务、反馈转行动和仿真自动决策增加失败关闭回归，禁止借用成员准入适配器。
- [x] 7.3 增加 real/simulation world 隔离测试，分别覆盖规则发布、快照、投票、判定和执行，确认另一个 world 的计数与角色事实不变。
- [x] 7.4 增加 OpenFGA store/model 缺失、请求失败、旧模型版本和 tuple 不完整测试，所有发起、投票与执行路径必须失败关闭。
- [x] 7.5 运行成员准入、角色、权限、workspace、observer、simulation 和统一事件账本定向测试，记录通过数与任何预期跳过原因。

## 8. 文档、部署与最终验收

- [x] 8.1 更新 Docs 数据库结构、架构总览、治理边界、成员工作台、Admin、observer、仿真、产品规划、路线图和 AI 开发指南，明确只有成员准入在本批恢复。
- [x] 8.2 更新 OpenFGA 模型版本、bootstrap、tuple rebuild 和探针；分别发布 real/sim 模型并给出人工更新 `.env` model id 的准确命令，不自动修改 `.env`。
- [x] 8.3 运行 `scripts/check_project.py --check-contracts`、Django check、迁移漂移、OpenSpec strict、Docs 中英文构建、孤立页面检查和两个仓库的 `git diff --check`。
- [x] 8.4 运行完整隔离回归并记录准确通过数和跳过项；不得把未运行或受外部环境阻塞的检查写成已通过。
- [x] 8.5 人工验收 real 与 simulation：配置明确政策、贡献者报名、合格成员投票与改票、达成结果、有权人员执行并获得一年期守约者资格；同时确认申请人自决、无权限执行和其他未迁移流程均被拒绝，浏览器及服务日志无 500、旧模型导入或跨 world 写入。
- [x] 8.6 在隔离 MySQL 测试数据库运行事件反馈与统一提案并发锁测试；CI 不得引用已删除的测试模块，且两项测试必须实际执行而非跳过。
