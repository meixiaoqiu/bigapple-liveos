## 1. 冻结删除边界

- [x] 1.1 生成旧模型、字段、表名、领域模块、URL、模板、命令、事件、OpenFGA、仿真、测试和 Docs/contract 引用目录，逐项标记“旧系统专属”“必须由新系统承接的制度语义”或“新系统同名概念”。
- [x] 1.2 为成员准入、角色任命与卸任、财务职责任命、社区共议、专业事务、管理事务、反馈和仿真流程记录发起资格、选民规则、阈值、截止条件、执行动作和审计要求，形成统一提案系统迁移缺口清单。
- [x] 1.3 建立旧提案残余机械检查并接入 `scripts/check_project.py`，测试产品代码、模板、迁移和公开契约新增旧标识时检查失败，同时允许 OpenSpec 过程文档和禁止目录本身记录历史。

## 2. 关闭未迁移运行入口

- [x] 2.1 定义统一“提案流程尚未迁移，暂不可用”领域状态和页面提示，测试未迁移流程不返回 500、不创建旧数据且不直接改变最终权威状态。
- [x] 2.2 从成员报名创建、详情、列表和执行链移除旧提案调用与字段展示，保留非提案报名信息并明确关闭准入决策操作。
- [x] 2.3 从角色任命、角色卸任和财务职责任命入口移除旧提案调用，测试这些流程失败关闭且不得直接授予或撤销角色。
- [x] 2.4 从 observer、Admin、管理命令、演示 seed、仿真推进、归档与失败处理移除旧提案调用，测试自动流程遇到未迁移决策时产生明确领域错误或关闭状态。

## 3. 删除旧运行时代码

- [x] 3.1 删除旧提案页面、API、URL、模板分支、Admin 注册、修复命令和仅服务旧提案的测试辅助入口，并验证不存在可反向解析的旧端点。
- [x] 3.2 删除 `core.proposals` 领域包、旧提案服务、选民评估运行实现、信号和旧事件 payload/ledger 投影，清理所有导入与导出。
- [x] 3.3 删除 OpenFGA tuple 重建、授权探针和角色基线中仅为旧提案服务的关系，同时保证其他权限检查继续失败关闭。
- [x] 3.4 删除仅证明旧系统实现的测试，保留制度规格并新增未迁移流程失败关闭测试和旧残余扫描测试。

## 4. 删除权威模型和数据库结构

- [x] 4.1 删除所有业务模型指向旧提案、投票、执行和旧选民规则的字段、反向关系、索引与约束，更新模型导出和相关查询。
- [x] 4.2 删除旧 `Proposal`、`ProposalVote`、`ProposalExecution`、旧选民规则及其专属辅助模型，验证 Django model graph 中不存在旧关系。
- [x] 4.3 重建受影响 app 的干净迁移基线，使空数据库从未创建旧表或字段；增加旧开发结构检测和明确的 world 重置说明，不编写旧数据转换或兼容迁移。
- [x] 4.4 在全新的 control、real、simulation 隔离数据库应用迁移并检查实际 schema，确认不存在登记的旧表、字段、索引或约束。

## 5. 同步契约和文档

- [x] 5.1 更新 sibling Docs 的 technical contracts、API、数据库结构、治理边界、架构总览、产品功能和开发指南，删除旧实现说明并标记暂时关闭的业务入口。
- [x] 5.2 保留 `configurable-electorate-rules` 等长期制度规格，把迁移缺口清单链接到后续统一提案系统变更，不将删除实现描述为取消治理制度。
- [x] 5.3 更新启动与重置说明，明确现有开发 world 必须重置，以及检测到旧 schema 时的准确处理命令。

## 6. 证明彻底移除

- [x] 6.1 运行旧残余扫描、`scripts/check_project.py`、Django check、迁移漂移检查和新旧 schema 断言，逐项清零所有非过程文档命中。
- [x] 6.2 运行成员报名、角色、财务、observer、仿真、OpenFGA 和未迁移关闭行为的定向测试，确认没有直接权威状态绕过。
- [x] 6.3 运行完整隔离回归、Docs 中英文构建、OpenSpec strict 和两个仓库的 `git diff --check`，如预期删除导致测试变更，必须逐项对应迁移缺口而不是批量跳过。
- [x] 6.4 人工验收 real 与 simulation：所有已登记未迁移流程显示明确关闭状态，已迁移到新系统的流程不引用旧代码，浏览器和服务日志无旧模型导入或服务器错误。
  - 2026-08-16 验收证据：real 匿名工作台和 simulation 管理员报名审核页均显示统一提案迁移期关闭说明；两端浏览器控制台无错误，请求返回 200。
  - real 与 simulation 分别调用成员准入提案创建服务，均抛出 `ProposalFlowUnavailable`；报名、审批提案、审批决定、角色任命和系统事件等权威表计数在调用前后保持一致。
  - 检查两端最近服务日志，未发现旧模型导入、HTTP 500 或 `Traceback`；旧提案残余扫描继续通过。
