## 1. 欢迎与状态摘要

- [x] 1.1 将黄色英雄卡替换为白色轻量欢迎区域，展示“你好，”和“欢迎回到工作台”，且不新增动态时间判断
- [x] 1.2 在欢迎文案之后实现细边框、无重阴影的双列状态摘要，左列使用现有成员状态，右列使用现有 `credit_balance`
- [x] 1.3 以紧凑元数据形式保留成员编号、资格或派生身份、职责、当前 world 与模拟日，不写死展示值

## 2. 现有内容重组

- [x] 2.1 将现有全部导航分组整体移动到欢迎、状态摘要和元数据之后，保持链接文字、URL、相对顺序及每项 Django 权限条件不变
- [x] 2.2 将后续核心状态区域收敛为可用积分与历史贡献，确认成员状态和当前积分只迁移位置、不重复且四项原有指标均未丢失
- [x] 2.3 确认“我的事务”、消息、待处理事项、近期积分流水及其他现有模块保持原有顺序和功能；快捷操作图标仅由完成本 change 后的独立 `redesign-workspace-quick-actions` 增量引入

## 3. 回归覆盖与文档

- [x] 3.1 增加 Workspace 页面测试，验证欢迎文案、现有状态值、积分值、身份与 world 上下文、四项指标各自唯一展示
- [x] 3.2 扩展不同权限成员的导航回归测试，验证导航下移后全部入口、URL、顺序和可见条件保持不变
- [x] 3.3 更新 `bigapple-docs/docs/product/member-workspace.md`，记录顶部欢迎与状态摘要的当前实现和渐进改造边界

## 4. 构建与验证

- [x] 4.1 运行 Tailwind 构建并提交更新后的 `theme/static/css/dist/styles.css`
- [x] 4.2 在窄屏和宽屏视口人工检查 480px 首页顶部，确认欢迎层级、双列摘要、元数据换行和下移导航没有横向溢出
- [x] 4.3 运行 Workspace 相关测试、Django check、OpenSpec strict validation 和两个仓库的 `git diff --check`
